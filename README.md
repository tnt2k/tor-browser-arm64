# tor-browser-arm64
Tor browser for arm64 Linux
INFO: There are issues on some SoCs with hardware acceleration support.

Install:

1) Extract (tar -xf tor-browser-linux-aarch64-14.5a1.tar.xz)
2) go to tor-browser folder (cd tor-browser)
3) ./start-tor-browser.desktop --register-app
